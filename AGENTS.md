# プロジェクト永続指示 (AGENTS.md)

## 必須運用ルール
1. **アップデート時のニュース更新義務**:
   - アプリケーションに機能追加、バランス調整、仕様変更、バグ修正などのアップデートを実施するたびに、必ず `/src/components/NewsModal.tsx` の `UPDATE_NEWS_LIST` の先頭に最新のお知らせ（日付、バージョン、タイトル、カテゴリ、具体的な変更内容）を追記すること。
   - 最新のお知らせは `isNew: true` とし、過去のお知らせは `isNew: false` に更新すること。
